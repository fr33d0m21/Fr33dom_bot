name: evey-mqtt
version: 2.0.0
description: Auto-connecting MQTT real-time event stream. Connects on plugin load, subscribes to bridge/events/health/mother topics, buffers messages.
provides_tools:
  - mqtt_subscribe
  - mqtt_publish_event
  - mqtt_status
